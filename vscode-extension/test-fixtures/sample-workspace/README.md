# Sample Workspace

A minimal, real project folder used only by the extension's own automated
tests (`@vscode/test-electron`) as the workspace VS Code opens during
testing. Not part of the shipped extension.
